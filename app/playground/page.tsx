export default function PlaygroundPage() {
  return (
    <main className="flex min-h-screen flex-col gap-8 p-8">
      <header>
        <h1 className="font-mono text-lg">Playground</h1>
        <p className="text-slate text-sm">
          All 16 pieces × all registered themes. Designer iteration surface.
        </p>
      </header>
      <section className="text-fog text-sm">
        Stub — populated in M2 when the Theme layer + piece geometry ship.
      </section>
    </main>
  );
}
