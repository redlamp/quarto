# 2026-05-26

## Done

- M3 polish bundle shipped as 5 feature branches merged to `dev` (not yet to `main` — awaiting review).
  - `feature/m3-gsap-catalog` — animation library (hover-lift, selection raise tween, placement drop with squash+bounce, handoff travel arc, win-line tube reveal with stagger cascade + non-winning dim, restart sweep, CSS confirm-pulse). New: `lib/motion/` (prefers-reduced-motion hook + duration mapper + tests).
  - `feature/m3-sound-sfx` — WebAudio synth port from `color-taylor/src/utils/audioContext.ts`. `lib/sfx/quartoSfx.ts` named generators: piece-pick blip, piece-place click+thunk, confirm tone, handoff rising sweep, quarto-call two-note, win-fanfare C-major triad. `useSfxBus` watches game state for transitions. Off by default, mute toggle in drawer.
  - `feature/m3-lighting-motion-pickers` — `ReactiveThemeProvider` threads zustand-persisted lighting + motion choices into ThemeProvider; settings drawer adds buttons per preset.
  - `feature/m3-clock` — pure clock math ported from `narrative-chess-v2/lib/chess/clock.ts` (LAG_CREDIT_MS + correspondence dropped). Presets Untimed/Blitz 3+0/5+0/Rapid 10+0/15+10, Fischer increment, adaptive tick rate, AI pause. HUD shows two clock displays above + below board when timed. Timeout enforcement (flag = loss) deferred.
  - `feature/m3-drag-to-confirm` — resumed [[backlog-drag-to-confirm]]. Handed piece on pedestal exposes a drag-grab cylinder. Pointer-down disables OrbitControls; pointer-up over an empty board cell runs selectCell + confirmPlace. Drag-to-pick (rack → opponent pedestal) deferred.

## Refs touched

- `narrative-chess-v2/lib/chess/clock.ts` — clock math pattern.
- `color-taylor/src/utils/audioContext.ts` — WebAudio singleton pattern.

## Gotchas learned

- React 19 `react-hooks/refs` blocks reading `ref.current` during render. Clock state had to migrate from `useRef` to `useState` + `setState` inside effect with local `react-hooks/set-state-in-effect` disable per the established pattern.
- `useReducedMotion` hook needed `useSyncExternalStore` (not `useEffect + setState`) to satisfy React 19's `react-hooks/set-state-in-effect` rule.
- `next dev` rewrites quotes in `next-env.d.ts` on every boot; revert before staging.

## Next

- User to review A-E end-to-end in browser before promoting to `main`.
- F (M4 — 2D fallback view + mobile) deferred until A-E lands on main.
- Open polish backlog: timeout-loss enforcement (D), drag-to-pick rack→pedestal (E), camera mode toggle (M3 unblocks camera crossfade animation already wired but inert).
