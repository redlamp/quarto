# 2026-05-27

## Done

- Shipped 2026-05-26 second-runthrough polish to `main` (`7f14478`).
- `feature/m3-timeout-loss` — closed the last clock gap: **timeout-loss enforcement** (was deferred in [[decision-clock-pattern-from-narrative-chess]]).
  - Engine: `QuartoState` gains `timeoutLoser` + `aborted`. New `flagFall` move (in both stages) sets the player on the clock as loser, or `aborted` if no move committed yet (PRD: first-move timeout = abort, no result). `endIf` resolves timeout → opponent wins, abort → `{ aborted: true }`.
  - UI: `game-shell` watches the active clock; at zero it dispatches `flagFall` once per game. AI never holds an active clock, so only a human can flag (AI keeps infinite time per design).
  - `WinBanner` shows "Player N wins on time" / "Game aborted — ran out of time on the first move".
  - +2 unit tests (abort vs loss). 41 unit tests pass, typecheck + lint clean.
  - **Not live-verified:** shortest preset is Blitz 3+0, too long to watch a real flag-fall in-browser; covered by unit tests only.

## Next

- F (M4 — 2D fallback view + mobile/responsive) — next milestone. Discussion pending before starting.
