# 2026-08-01

- Variants take 2 ([[decision-trait-catalog-binary]]): every trait binary; Quinto/Sesto add _new_ traits (hue, girth) instead of ternary/5-value expressions. Built a 9-trait catalog (canonical four + hue, girth, band, stripes, base) with conflict metadata (band ↔ stripes).
- Trait picker in the settings drawer: per-board-size selection, persisted, conflicts disable with inline reason, min 2 traits, reset-to-default. Top bar now switches board size (2×2 Secondo … 6×6 Sesto).
- Piece renderer: hue tint (orange/blue × light/dark), stripes (two thin rings), plinth base; prism silhouettes removed with the mixed-arity model.
- Fixes surfaced by e2e: settings sheet lacked `overflow-y-auto` (Reset button unreachable once Traits group landed); 5×5 world scale 0.8 → 0.74 (8-col rack clipped the frame).
- Gates: typecheck, lint, 57 unit, 5 e2e (new trait-picker spec) pass; screenshots verified incl. a custom band+base set.
- Hollow pieces carved for real: round pieces lathe a bore into the profile (rim fillet → cavity wall → floor); square pieces = solid lower body + extruded wall ring with circular bore (ring overlaps the body's edge fillet). The old grey "gem" plug is gone.
- Coupled board size to trait count (toggle a trait → board gains/loses a row+column, fresh game). Clarified the n² vs 2^n arithmetic in [[decision-trait-catalog-binary]] — counts only align at n=2 and n=4; surplus pieces at 5/6 are fine (game ends on board-full).
- Opacity trait added (opaque/clear): clear pieces are transmission glass, deliberately distinct from the ghost preview's opacity fade. 10-trait catalog now.
